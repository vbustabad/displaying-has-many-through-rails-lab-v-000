<h1>Doctor#show</h1>

<ol>
  <% @doctor.appointments.each do |appointment| %>
    <li><%= appointment.appointment_datetime.to_formatted_s(:long) %></li><br />
    <span><%= link_to appointment.patient.name, patient_path(appointment.patient) %></span><br />
  <% end %>
</ol>
